<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Truyenchu"
        tools:targetApi="31">
        <activity
            android:name=".activity.TaoBaiDangActivity"
            android:exported="false" />

        <activity
            android:name=".activity.TimKiemActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ChiTietTruyenActivity"
            android:exported="false" />
        <!--
         THAY ĐỔI QUAN TRỌNG:
         Khai báo MainActivity nằm ở thư mục gốc.
         Dấu "." ở đầu nghĩa là nó nằm trong package gốc (com.example.truyenchu)
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>